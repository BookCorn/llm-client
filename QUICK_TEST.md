# 快速测试指南

## ✅ 已修复的问题

1. **推理摘要重复显示** - 不再重复更新，只显示一个推理框
2. **性能优化** - 添加100ms节流，减少UI重绘频率
3. **详细调试输出** - 显示API返回的完整内容

## 🚀 立即测试

```bash
export OPENAI_USE_RESPONSES_API=true
cargo run
```

## 📊 查看调试输出

运行应用后，控制台会显示：

### 1. API返回的完整内容

```
📄 完整响应内容预览:
# Markdown Test

This is **bold** and *italic* text...
─────────────────────────────────────
```

**关键检查点**:
- ✅ 如果看到 `#`, `**`, `*` → API返回了Markdown
- ❌ 如果只有纯文本 → API没有返回Markdown

### 2. 推理摘要内容

```
🧠 完整推理摘要预览:
**Explaining concurrency issues**

I'm looking at deadlock...
─────────────────────────────────────
```

**关键检查点**:
- ✅ 只显示一次
- ✅ 包含完整内容

## 🔍 Markdown格式诊断

### 测试消息

发送这条消息：

```
请用Markdown格式回复，包括：
# 一级标题
## 二级标题
**加粗文字**
*斜体文字*
```

### 预期结果

**控制台输出**应该显示：

```
📄 完整响应内容预览:
# Title
## Subtitle
This is **bold** and *italic*
```

**如果控制台显示纯文本**（没有`#`、`**`、`*`）：

→ **原因**: API不返回Markdown格式
→ **解决**: 可能需要切换API提供商或模型

**如果控制台有Markdown但UI不显示**：

→ **原因**: TextView渲染问题
→ **解决**: 我会继续调查TextView配置

## 📋 测试清单

运行应用后，测试以下内容：

- [ ] 推理摘要只显示一个框（不重复）
- [ ] 推理摘要可以展开/折叠
- [ ] 推理摘要展开后不与主内容重叠
- [ ] 滚动页面流畅（不卡顿）
- [ ] 查看控制台的 `📄 完整响应内容预览`
- [ ] 确认API是否返回Markdown格式

## 🐛 如果还有问题

### 问题1: 推理摘要还是重复

**截图**: 显示两个推理框的界面
**信息**: 告诉我看到几个推理框

### 问题2: 滚动还是卡顿

**测试**: 来回滚动页面
**信息**: 告诉我FPS大约多少（流畅/略卡/很卡）

### 问题3: Markdown没有渲染

**关键信息**:

1. **查看控制台输出** - 截图 `📄 完整响应内容预览` 部分
2. **告诉我**:
   - 控制台是否显示Markdown符号（`#`, `**`, `*`）
   - UI是否显示格式（标题、加粗、斜体）

## 📝 反馈格式

请提供：

```
1. 推理摘要显示：[正常/重复/缺失]
2. 滚动性能：[流畅/略卡/很卡]
3. 控制台Markdown符号：[有/无]
4. UI Markdown渲染：[有/无]
5. 截图：[控制台输出 + UI显示]
```

---

**更新**: 2025-01-05
**版本**: v0.2 - 性能优化版
